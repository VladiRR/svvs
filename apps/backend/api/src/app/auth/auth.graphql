extend type Query {
  "It's login query demo description"
  login(
    "Username it's name current user"
    username: String!,
    "password it's password"
    password: String!
  ): SignInResponse
  logout: Boolean
}

"""
Demo
multi-line
comment
"""
type SignInResponse {
  accessToken: String
  expiresIn: Int
  id: Int
}
